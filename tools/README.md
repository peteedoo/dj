# Tools

Utility scripts live here. Ideas:

- **serato-export.py** — parse Serato history XML into markdown set logs
- **bpm-tag.sh** — batch BPM analysis via `bpm-tools` or `aubio`
- **find-dupes.sh** — detect duplicate tracks across NAS and SSD
- **genre-scan.sh** — scan NAS music library and list folders missing genre tags
